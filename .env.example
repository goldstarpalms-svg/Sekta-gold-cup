# Optional API keys for the SEKTA GOLD AI Streamlit chatbot.
# Copy to .env for local development. Do not commit real secrets.

# Add at least one provider key for real AI responses.
GROQ_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=

# Optional: better web results when "Add web context" is enabled.
TAVILY_API_KEY=

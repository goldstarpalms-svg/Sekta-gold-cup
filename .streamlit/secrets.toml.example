# Copy this to secrets.toml locally OR paste into Streamlit Cloud → Secrets
# NEVER commit real secrets.toml
# Get your NEW key after revoking leaked one at https://platform.openai.com/api-keys

OPENAI_API_KEY = "sk-proj-YOUR_NEW_KEY_HERE_AFTER_REVOKE"
TAVILY_API_KEY = "tvly-... optional but recommended for real web search"
OPENAI_MODEL = "gpt-4o"
